import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Desativado temporariamente porque o build acusou falta do módulo 'critters'
  // experimental: { optimizeCss: true },
};

export default nextConfig;
